Here's an example of a basic code structure for creating a car rental site using HTML, CSS, and JavaScript. Please note that this is a simplified example and may require additional functionality and security measures for a real-world application.

**HTML (index.html)**
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car Rental Site</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="#">About</a></li>
                <li><a href="#">Contact</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <section>
            <h1>Car Rental Site</h1>
            <form id="car-rental-form">
                <label for="pickup-location">Pickup Location:</label>
                <select id="pickup-location" name="pickup-location">
                    <option value="airport">Airport</option>
                    <option value="city-center">City Center</option>
                </select>
                <br>
                <label for="dropoff-location">Dropoff Location:</label>
                <select id="dropoff-location" name="dropoff-location">
                    <option value="airport">Airport</option>
                    <option value="city-center">City Center</option>
                </select>
                <br>
                <label for="pickup-date">Pickup Date:</label>
                <input type="date" id="pickup-date" name="pickup-date" required>
                <br>
                <label for="dropoff-date">Dropoff Date:</label>
                <input type="date" id="dropoff-date" name="dropoff-date" required>
                <br>
                <label for="car-type">Car Type:</label>
                <select id="car-type" name="car-type">
                    <option value="economy">Economy</option>
                    <option value="compact">Compact</option>
                    <option value="intermediate">Intermediate</option>
                    <option value="standard">Standard</option>
                    <option value="full-size">Full-size</option>
                    <option value="luxury">Luxury</option>
                </select>
                <br>
                <button id="search-cars">Search Cars</button>
            </form>
            <div id="car-results"></div>
        </section>
    </main>
    <script src="script.js"></script>
</body>
</html>
```

**CSS (styles.css)**
```css
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

header {
    background-color: #333;
    color: #fff;
    padding: 1em;
    text-align: center;
}

nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: space-between;
}

nav li {
    margin-right: 20px;
}

nav a {
    color: #fff;
    text-decoration: none;
}

main {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2em;
}

section {
    background-color: #f7f7f7;
    padding: 1em;
    border: 1px solid #ddd;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

h1 {
    margin-top: 0;
}

label {
    display: block;
    margin-bottom: 10px;
}

input[type="date"] {
    width: 100%;
    height: 40px;
    margin-bottom: 20px;
    padding: 10px;
    border: 1px solid #ccc;
}

select {
    width: 100%;
    height: 40px;
    margin-bottom: 20px;
    padding: 10px;
    border: 1px solid #ccc;
}

button {
    background-color: #333;
    color: #fff;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    background-color: #444;
}

#car-results {
    margin-top: 20px;
}

.car-result {
    background-color: #fff;
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
}

.car-result img {
    width: 100px;
    height: 100px;
    margin-right: 20px;
}

.car-result h2 {
    margin-top: 0;
}

.car-result p {
    margin-bottom: 20px;
}

.car-result button {
    background-color: #333;
    color: #fff;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
}

.car-result button:hover {
    background-color: #444;
}
```

**JavaScript (script.js)**
```javascript
const carRentalForm = document.getElementById('car-rental-form');
const pickupLocationSelect = document.getElementById('pickup-location');
const dropoffLocationSelect = document.getElementById('dropoff-location');
const pickupDateInput = document.getElementById('pickup-date');
const dropoffDateInput = document.getElementById('dropoff-date');
const carTypeSelect = document.getElementById('car-type');
const searchCarsButton = document.getElementById('search-cars');
const carResultsDiv = document.getElementById('car-results');

searchCarsButton.addEventListener('click', (e) => {
    e.preventDefault();
    const pickupLocation = pickupLocationSelect.value;
    const dropoffLocation = dropoffLocationSelect.value;
    const pickupDate = pickupDateInput.value;
    const dropoffDate = dropoffDateInput.value;
    const carType = carTypeSelect.value;
    const cars = getCars(pickupLocation, dropoffLocation, pickupDate, dropoffDate, carType);
    renderCars(cars);
});

function getCars(pickupLocation, dropoffLocation, pickupDate, dropoffDate, carType) {
    // This is a mock API call, you should replace it with a real API call
    const cars = [
        {
            id: 1,
            name: 'Toyota Corolla',
            type: 'Economy',
            price: 40,
            image: 'https://via.placeholder.com/100x100'
        },
        {
            id: 2,
            name: 'Honda Civic',
            type: 'Compact',
            price: 50,
            image: 'https://via.placeholder.com/100x100'
        },
        {
            id: 3,
            name: 'Ford Focus',
            type: 'Intermediate',
            price: 60,
            image: 'https://via.placeholder.com/100x100'
        },
        {
            id: 4,
            name: 'Nissan Altima',
            type: 'Standard',
            price: 70,
            image: 'https://via.placeholder.com/100x100'
        },
        {
            id: 5,
            name: 'BMW 3 Series',
            type: 'Luxury',
            price: 100,
            image: 'https://via.placeholder.com/100x100'
        }
    ];
    return cars.filter((car) => car.type === carType);
}

function renderCars(cars) {
    carResultsDiv.innerHTML = '';
    cars.forEach((car) => {
        const carResult = document.createElement('div');
        carResult.classList.add('car-result');
        carResult.innerHTML = `
            <img src="${car.image}" alt="${car.name}">
            <h2>${car.name}</h2>
            <p>Price: $${car.price} per day</p>
            <button>Rent Now</button>
        `;
        carResultsDiv.appendChild(carResult);
    });
}
```

This code creates a basic car rental site with a form that allows users to input their pickup and dropoff locations, dates, and car type. The site then displays a list of available cars that match the user's search criteria.

Please note that this is a simplified example and does not include any error handling, validation, or security measures. You should consider adding these features to make the site more robust and secure.

Also, this code uses a mock API call, you should replace it with a real API call to a car rental company's API.

You can also use a library like `react` or `angular` to build the site, it will make it more scalable and maintainable.

Please let me know if you have any questions or need further assistance.